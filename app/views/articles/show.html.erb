<%# <h2>= @article.title</h2>
<%# <p>= @article.content %>

<%#= link_to "Back", articles_path %> |
<%#= link_to "Edit", edit_article_path(@article) %>
<%#= link_to "Delete",
  article_path(@article),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>


<h2 class="article-title text-center mb-4"><%= @article.title %></h2>
<div class="article-content">
  <p><%= @article.content %></p>
  <div class="action-links mt-3">
    <%= link_to "Back", articles_path, class: "btn btn-custom btn-sm" %> |
    <%= link_to "Edit", edit_article_path(@article), class: "btn btn-custom btn-sm" %> |
    <%= link_to "Delete", article_path(@article), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-danger btn-sm" %>
  </div>
</div>
